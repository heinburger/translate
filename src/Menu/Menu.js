import React from 'react';
import Header from './Header';
import Divider from '@material-ui/core/Divider';

const Menu = () => (
  <React.Fragment>
    <Header />
    <Divider />
    <div>
      menu
    </div>
  </React.Fragment>
);

export default Menu;
